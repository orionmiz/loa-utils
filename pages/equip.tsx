import Layout from "components/layout";
import { observer } from "mobx-react-lite";

function EquipPage() {
  return (
    <Layout>
      <div className="m-4">
        🚧
      </div>
    </Layout>)
}

export default observer(EquipPage);